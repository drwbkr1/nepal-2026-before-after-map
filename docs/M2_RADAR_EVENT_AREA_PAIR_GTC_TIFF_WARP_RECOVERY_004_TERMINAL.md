# M2 radar GTC–TIFF–Warp recovery-004: final preflight stop

The owner approved the exact recovery-004 packet as one conditional envelope. Its separate implementation passed 862 portable tests locally (six intentional skips), a generated ArcGIS/GDAL bridge and exact-grid check, and public CI at commit `e4de8ab898fc3a49f45b13fe3ae7474aacde55dd` in run `36262383446`. The latest generated check compares the actual production bridge function and reports no project-data access. These tests establish method mechanics on disposable rasters only.

The final no-content preflight stopped with `event_pair_attempt_root_collision`. The proposal specified `e1/a3` only if absent, but [the earlier public terminal](../records/readiness/m2-map-route-feasibility-001-terminal-publication.json) already records `e1/a3` as consumed on 25 September. A root-level check confirmed the directory already exists; no contents were opened or changed. No pass preflight receipt was written, no recovery-004 radar worker launched, and no protected radar pixels were read. [Exact sanitized preflight record](../records/readiness/m2-radar-event-area-pair-gtc-tiff-warp-recovery-004-final-preflight-terminal.json).

This was a control error in the reviewed packet: it reused an occupied attempt identity. The old `e1/a3` attempt stopped at `native_declared_resource_limit_exceeded` before the proposed TIFF bridge, so changing the alias alone would not resolve that upstream guard. The bridge test does not show that a new real radar attempt would pass. The approved envelope has reached its hard stop; it permits no alternate identity, retry, source substitution, or relaxation of the native resource limit.

Neither the radar pair nor the blocked optical pair has passed before/after pixel QA. No baseline, change raster, interpretation, attribution, derived-pixel publication, or scientific map claim follows from this work. Any new map route needs a separate, exact owner decision tied to the existing terminal evidence.
