name: Validate project controls

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install pinned portable QA dependencies
        run: python -m pip install --disable-pip-version-check --no-deps numpy==2.5.1 blake3==1.0.9
      - name: Validate repository
        run: python scripts/check_project.py
      - name: Test project controls
        run: python -m unittest discover -s tests -v
